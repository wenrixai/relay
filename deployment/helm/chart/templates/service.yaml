apiVersion: v1
kind: Service
metadata:
  name: {{ include "wenrix-relay.fullname" . }}
  labels:
    {{- include "wenrix-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "wenrix-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
